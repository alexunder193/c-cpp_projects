#ifndef __STACK__
#define __STACK__
typedef struct alex *typos_stoivas;
typos_stoivas dimiourgia();
int keni(const typos_stoivas stoiva);
void stoiva_PopValue(const typos_stoivas stoiva,int *stoixeioptr);
void othisi(const typos_stoivas *stoiva,int stoixeio);
#endif 
